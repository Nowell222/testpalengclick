/**
 * VendorBottomNav — replaced by the bottom nav built into DashboardLayout.
 * This stub exists so existing imports in page files don't break.
 * DashboardLayout already renders the correct dark pill bottom nav for mobile.
 */

interface Props {
  unreadNotifs?: number;
}

const VendorBottomNav = (_props: Props) => null;

export default VendorBottomNav;
